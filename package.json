{
  "name": "maavarim",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-admin": "PUBLIC_URL=/admin/ npm run --prefix admin build && mv admin/build/ dist/admin",
    "deploy": "rm -rf dist && mkdir dist && npm run build-admin && cp -r public/* dist && gh-pages -d dist -b master && rm -rf dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maavarim/maavarim.github.io.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/maavarim/maavarim.github.io/issues"
  },
  "homepage": "https://github.com/maavarim/maavarim.github.io#readme",
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
